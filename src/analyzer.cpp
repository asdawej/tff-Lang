#include "tff.h"
#include <fstream>
#include <string>
#include <vector>
using namespace std;

// 分析器主函数
vector<string> analyzer(const char *tffl_file) {
    ifstream text_code(tffl_file);
    text_code.close();
    return std::vector<std::string>();
}